# Shared environment configuration for backend (Node) and frontend (Vite)

# Optional M365 Authentication (default: true)
AUTH_ENABLED=true

# Azure Entra ID (tenant and client) – identifiers only, safe for frontend
AZURE_TENANT_ID=your-tenant-guid-or-domain
AZURE_CLIENT_ID=your-azure-client-id

# Frontend-safe copies (Vite only exposes VITE_* to browser)
VITE_AZURE_TENANT_ID=your-tenant-guid-or-domain
VITE_AZURE_CLIENT_ID=your-azure-client-id

# Personio API (backend only – secrets, do NOT prefix with VITE_)
PERSONIO_CLIENT_ID=your-personio-client-id
PERSONIO_CLIENT_SECRET=your-personio-client-secret

# CORS / frontend origin for the Node backend
ALLOWED_ORIGIN=http://localhost:3000
PORT=8080
# Optional M365 Authentication (default: true)
AUTH_ENABLED=true
COMPANY_NAME=Organization
